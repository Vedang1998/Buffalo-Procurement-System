Phase 3 is approved. The four Shopify secrets are already configured in Replit Secrets. Proceed with the live catalog reconciliation, subject to the requirements below.

A. Shopify connection validation

1. Use the existing client-credentials implementation. Do not log, expose, persist to source, or display the client secret or generated Shopify access token.
2. Validate authentication with a harmless read-only request before starting the catalog sync.
3. Confirm the app has the required access for the catalog fields actually being queried. If Shopify returns an access-scope or authentication error, stop and report it rather than modifying permissions or architecture yourself.
4. Continue using SHOPIFY_API_VERSION=2026-07.

B. Define the live catalog correctly

Do not treat an unfiltered productVariantsCount result as the authoritative active-catalog definition.

Retrieve the complete current purchasing catalog using Shopify’s explicit active-product filter:

product_status:ACTIVE

Paginate through the entire result set and independently verify the number of records actually retrieved.

Persist the catalog-sync run with:

* run ID
* Shopify API version
* started/completed timestamps
* total active variants fetched
* pagination completion status
* source-record count
* deterministic snapshot/hash where supported by the current architecture
* reconciliation summary

A count difference from the historical seed is evidence to reconcile, not an error by itself.

C. Canonical identity rules remain non-negotiable

Shopify Variant ID is the canonical current identity.

Classify the comparison between the 2,029 historical seed identities and the full live Shopify active catalog into at least:

1. EXACT_CURRENT_ID
2. NEW_CURRENT_VARIANT
3. HISTORICAL_VARIANT_MISSING_FROM_CURRENT
4. RECREATION_CANDIDATE
5. METADATA_CHANGED
6. AMBIGUOUS_IDENTITY

Exact Variant-ID matches may reconcile automatically.

A genuinely new live Shopify Variant ID may be represented as a new canonical current identity. That is not permission to connect it to an old historical Variant ID.

Never automatically create an old-ID → new-ID alias.

Never automatically retire a historical identity.

Never use fuzzy title similarity alone to establish identity continuity.

D. Recreation-candidate evidence

For every possible recreated product, show the old historical record beside the new Shopify record with as much available evidence as possible:

* old Variant ID
* new Variant ID
* old/current SKU
* barcode
* product title
* variant title / size
* product handle where useful
* product/vendor
* inventory item ID where available
* price
* created/updated timestamps
* matching evidence
* conflicting evidence
* confidence/evidence classification

Strong SKU/barcode/title evidence may nominate a candidate, but it does not authorize an alias.

Be especially conservative where size, pack, vintage, proof, ABV, barcode, or product identity conflicts.

E. Human review actions

The Catalog Reconciliation UI must support at minimum:

APPROVE RECREATION

* human explicitly confirms old Variant ID and new Variant ID represent continuity of the same sellable item;
* persist permanent historical alias/mapping;
* preserve all historical sales under the canonical current Variant ID;
* record actor, timestamp, evidence and decision.

MARK HISTORICAL IDENTITY RETIRED

* means retired only inside Buffalo Procurement’s identity model;
* must not archive/delete/change anything in Shopify;
* historical data remains preserved.

REJECT RECREATION / KEEP SEPARATE

* old and new identities are different products;
* persist enough evidence so the same bad candidate isn’t repeatedly suggested.

LEAVE UNRESOLVED

* remains a blocker where continuity is materially uncertain.

Identity decisions are PERMANENT/audited decisions, not RUN_ONLY decisions.

F. Fail-closed CATALOG_SYNC acceptance criteria

CATALOG_SYNC may become PASS only when the implementation can prove that:

1. the full active Shopify catalog was fetched successfully;
2. every current live Variant ID is represented correctly in the canonical catalog;
3. every historical seed identity that requires disposition is either:
    * still the exact current Variant ID,
    * linked through an explicitly human-approved recreation alias,
    * explicitly human-confirmed as retired/separate,
    * or otherwise handled according to the canonical specification;
4. no material ambiguous recreation candidates remain unresolved;
5. there are no duplicate/conflicting canonical identity relationships;
6. no material size/pack/barcode identity conflicts are silently accepted;
7. the reconciliation run and all human decisions are auditable.

Do not change the gate simply to make the count match 2,029, 1,998, 2,003, or any previous observation.

G. Important boundary

Phase 3 remains READ-ONLY against Shopify.

Do not:

* update Shopify SKUs;
* change products;
* change inventory;
* archive products;
* create POs;
* change retail prices;
* alter supplier mappings unrelated to catalog identity.

Any “retired” operation in this phase is Procurement-OS metadata only.

H. Testing

Add adversarial tests covering at least:

* exact Variant-ID match;
* old ID missing + one strong new candidate;
* same SKU attached to multiple current variants;
* matching title but different size;
* matching SKU but conflicting barcode;
* matching barcode but changed SKU;
* old historical ID with no replacement;
* genuinely new product;
* multiple old IDs correctly converging through already-approved historical aliases;
* rejected candidate does not reappear as automatically approved;
* no identity decision can be auto-promoted because of a confidence score.

Preserve all existing passing tests.

I. Stop point

Run the live catalog sync and build/populate the reconciliation review flow.

Do not begin SALES_BACKFILL / Phase 4 yet.

At the Phase 3 stop point report:

* live active Shopify variant count;
* exact-ID matches;
* genuinely new current variants;
* historical IDs missing from current;
* recreation candidates;
* ambiguous identities;
* metadata-only changes;
* duplicate SKU conflicts;
* barcode conflicts;
* unresolved blocker count;
* tests passed;
* migrations/files changed;
* current CATALOG_SYNC state;
* every reason preventing PASS if it remains FAIL.

If human decisions are required, leave CATALOG_SYNC=FAIL, present the review queue, and stop for my review.

Proceed with Phase 3 now.