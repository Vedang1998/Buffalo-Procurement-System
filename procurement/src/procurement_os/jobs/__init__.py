"""Scheduled/one-shot Procurement OS jobs.

Each job is a plain Python module so Replit Scheduled Deployments can invoke it directly
and another scheduler can replace Replit without changing domain code.
"""
